#include <stdio.h>
#include <stdlib.h>

int main()
{
	int ph,cp;
	printf("No. of Philosophers:");
	scanf("%d",&ph);
	printf("No. of chopsticks:");
	scanf("%d",&cp);
	int chopsticks[ch];
	do{
		wait(chopstick[i]);
		wait(chopstick[(i+1)%ph]);
		//eat
		signal(chopstick[i]);
		signal(chopstick[(i+1)%ph]);
		//think

    }while(true);

	
}
